apply from: "$rootDir/common-android-library.gradle"

android {
    namespace = "com.internal.repository"
}

dependencies {
    implementation(project(Modules.data))
    implementation(Dependencies.kotlinCoroutinesCore)
}
